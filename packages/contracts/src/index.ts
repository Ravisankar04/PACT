/** Contract package — addresses loaded from env / deployments/local.json after forge script. */
export const CONTRACT_NAMES = [
  "MockUSDC",
  "AgentRegistry",
  "PolicyManager",
  "AgentVault",
  "Escrow",
  "ReputationRegistry",
  "MockFlightProvider",
] as const;
